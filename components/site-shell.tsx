'use client'
import Link from 'next/link'; import Image from 'next/image'; import { Menu, Moon, Sun, ArrowUpRight, MessageCircle } from 'lucide-react'; import { useState } from 'react'; import { useTheme } from './theme-provider'; import { siteConfig } from '@/lib/data'
export function Header(){const [open,setOpen]=useState(false); const {dark,toggle}=useTheme(); const links=[['Menu','/menu'],['Our story','/about'],['Locations','/locations'],['Gallery','/gallery'],['Contact','/contact']]; return <header className="sticky top-0 z-40 border-b border-border/70 bg-background/90 backdrop-blur"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6"><Link href="/" className="flex items-center gap-2 font-black tracking-tight"><span className="grid size-9 place-items-center rounded-full bg-primary text-primary-foreground">M</span><span>{siteConfig.name}</span></Link><nav className="hidden items-center gap-6 md:flex">{links.map(([label,href])=><Link key={href} href={href} className="text-sm text-muted-foreground hover:text-foreground">{label}</Link>)}<Link href="/book" className="rounded-full bg-primary px-4 py-2 text-sm font-bold text-primary-foreground">Book a table</Link><button aria-label="Toggle theme" onClick={toggle} className="rounded-full border p-2">{dark?<Sun size={16}/>:<Moon size={16}/>}</button></nav><div className="flex items-center gap-2 md:hidden"><button aria-label="Toggle theme" onClick={toggle} className="rounded-full border p-2">{dark?<Sun size={16}/>:<Moon size={16}/>}</button><button aria-label="Open navigation" onClick={()=>setOpen(!open)} className="rounded-full border p-2"><Menu size={18}/></button></div></div>{open&&<nav className="flex flex-col gap-1 border-t px-4 py-3 md:hidden">{links.map(([label,href])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="rounded-lg px-3 py-3 text-sm font-medium">{label}</Link>)}<Link href="/book" className="rounded-lg bg-primary px-3 py-3 text-sm font-bold text-primary-foreground">Book a table</Link></nav>}</header>}
export function Footer(){return <footer className="border-t border-border"><div className="mx-auto grid max-w-7xl gap-10 px-4 py-12 sm:px-6 md:grid-cols-[1.4fr_1fr_1fr]"><div><div className="mb-3 flex items-center gap-2 font-black"><span className="grid size-8 place-items-center rounded-full bg-primary text-primary-foreground">M</span>{siteConfig.name}</div><p className="max-w-xs text-sm text-muted-foreground">{siteConfig.tagline} Verified restaurant details will be connected from the approved configuration.</p></div><div><p className="mb-3 text-sm font-bold">Explore</p><div className="flex flex-col gap-2 text-sm text-muted-foreground"><Link href="/menu">Menu</Link><Link href="/about">Our story</Link><Link href="/locations">Locations</Link><Link href="/gallery">Gallery</Link></div></div><div><p className="mb-3 text-sm font-bold">Connect</p><div className="flex flex-col gap-2 text-sm text-muted-foreground"><Link href="/order">Order online <ArrowUpRight className="inline" size={14}/></Link><Link href="/contact">Contact</Link><span className="inline-flex items-center gap-2"><MessageCircle size={14}/> WhatsApp pending approval</span></div></div></div><div className="border-t px-4 py-5 text-center text-xs text-muted-foreground">© {new Date().getFullYear()} {siteConfig.name}. Built for backend connection.</div></footer>}
export function Shell({children}:{children:React.ReactNode}){return <><Header/><main>{children}</main><Footer/></>}
export function CategoryImage({src,alt,className}:{src:string;alt:string;className?:string}){return <Image src={`/images/categories/${src}`} alt={alt} width={900} height={650} className={className||'h-full w-full object-cover'} />}
