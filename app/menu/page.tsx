import Link from 'next/link'; import { categories, categoryImage, formatPrice, menu } from '@/lib/data'; import { CategoryImage, Shell } from '@/components/site-shell'
export default function MenuPage(){return <Shell><div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 md:py-20"><p className="text-sm font-bold uppercase tracking-[.2em] text-primary">The menu</p><h1 className="mt-3 max-w-2xl text-5xl font-black tracking-tight sm:text-7xl">Made fresh, <span className="text-primary">worth sharing.</span></h1><p className="mt-5 max-w-xl text-muted-foreground">Approved menu data is centralized and ready to be replaced by the backend service.</p><div className="sticky top-16 z-20 -mx-4 mt-8 overflow-x-auto border-y bg-background/95 px-4 py-3 backdrop-blur sm:mx-0 sm:rounded-full sm:border"> <div className="flex min-w-max gap-2">{categories.map(c=><a key={c.id} href={`#${c.id}`} className="rounded-full border px-4 py-2 text-sm font-semibold">{c.name}</a>)}</div></div><div className="mt-12 flex flex-col gap-16">{categories.map(c=><section id={c.id} key={c.id} className="scroll-mt-32"><div className="mb-6 flex items-end justify-between gap-4"><div><h2 className="text-2xl font-black sm:text-3xl">{c.name}</h2><p className="mt-1 text-sm text-muted-foreground">{c.description}</p></div><div className="hidden h-14 w-20 overflow-hidden rounded-lg sm:block"><CategoryImage src={c.image} alt=""/></div></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{menu.filter(item=>item.category===c.id).map(item=><article key={item.id} className="flex items-start justify-between gap-4 rounded-2xl border bg-card p-4"><div><h3 className="font-bold">{item.name}</h3><p className="mt-1 text-xs text-muted-foreground">{item.vegetarian?'Vegetarian · ':''}{item.available?'Available':'Currently unavailable'}</p></div><p className="shrink-0 font-black text-primary">{formatPrice(item.price)}</p></article>)}</div></section>)}</div></div></Shell>}
