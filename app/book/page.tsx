import { BookingForm } from '@/components/booking-form'; import { Shell } from '@/components/site-shell'
export default function Book(){return <Shell><div className="mx-auto max-w-3xl px-4 py-12 sm:px-6 md:py-20"><p className="text-sm font-bold uppercase tracking-[.2em] text-primary">Book a table</p><h1 className="mt-3 text-5xl font-black tracking-tight sm:text-7xl">Make it a meal to remember.</h1><p className="mt-5 mb-10 text-muted-foreground">Send a table request. Final confirmation will be handled by the connected restaurant booking system.</p><BookingForm/></div></Shell>}
