import Link from 'next/link'; import { CalendarDays, ChefHat, MapPin, Settings2 } from 'lucide-react'; import { menu } from '@/lib/data'
export default function Admin(){return <main className="min-h-screen bg-muted/40"><div className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div><p className="text-sm font-bold uppercase tracking-[.2em] text-primary">Admin portal</p><h1 className="mt-2 text-4xl font-black tracking-tight">Restaurant operations</h1></div><Link href="/" className="text-sm font-bold underline">View website</Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Menu items',menu.length,ChefHat],['Bookings','API ready',CalendarDays],['Locations','Config ready',MapPin],['Content','API ready',Settings2]].map(([label,value,Icon])=><div key={label as string} className="rounded-2xl border bg-card p-5"><Icon className="text-primary" size={20}/><p className="mt-5 text-sm text-muted-foreground">{label as string}</p><p className="mt-1 text-2xl font-black">{value as string}</p></div>)}</div><section className="mt-8 rounded-3xl border bg-card p-5 sm:p-8"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><h2 className="text-2xl font-black">Menu management</h2><p className="mt-1 text-sm text-muted-foreground">Edit, add and toggle availability through the backend service layer.</p></div><button className="rounded-full bg-primary px-4 py-2 text-sm font-bold text-primary-foreground">Add dish</button></div><div className="mt-6 overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="border-b text-muted-foreground"><tr><th className="pb-3">Dish</th><th className="pb-3">Category</th><th className="pb-3">Price</th><th className="pb-3">Availability</th><th className="pb-3">Action</th></tr></thead><tbody>{menu.slice(0,10).map(item=><tr key={item.id} className="border-b last:border-0"><td className="py-4 font-semibold">{item.name}</td><td className="py-4 text-muted-foreground">{item.category}</td><td className="py-4">₹{item.price}</td><td className="py-4"><span className="rounded-full bg-primary/15 px-2 py-1 text-xs font-semibold">{item.available?'Available':'Unavailable'}</span></td><td className="py-4"><button className="font-semibold underline">Edit</button></td></tr>)}</tbody></table></div></section><p className="mt-5 text-xs text-muted-foreground">Authentication is intentionally not simulated. Connect backend auth before granting access in production.</p></div></main>}
