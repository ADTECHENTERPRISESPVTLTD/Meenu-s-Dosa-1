import { categories } from '@/lib/data'; import { CategoryImage, Shell } from '@/components/site-shell'
export default function Gallery(){return <Shell><div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 md:py-20"><p className="text-sm font-bold uppercase tracking-[.2em] text-primary">Gallery</p><h1 className="mt-3 text-5xl font-black tracking-tight sm:text-7xl">A taste of the table.</h1><div className="mt-10 grid gap-4 sm:grid-cols-2 md:grid-cols-3">{categories.slice(0,6).map(c=><div key={c.id} className="overflow-hidden rounded-3xl"><CategoryImage src={c.image} alt={c.name}/><p className="bg-card p-4 font-bold">{c.name}</p></div>)}</div></div></Shell>}
