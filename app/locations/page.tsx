import { MapPin, Phone, Clock, ExternalLink } from 'lucide-react'; import { locations } from '@/lib/data'; import { Shell } from '@/components/site-shell'
export default function Locations(){return <Shell><div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 md:py-20"><p className="text-sm font-bold uppercase tracking-[.2em] text-primary">Find us</p><h1 className="mt-3 text-5xl font-black tracking-tight sm:text-7xl">Come hungry.</h1><p className="mt-5 max-w-xl text-muted-foreground">Official locations and opening details will appear here once verified by the restaurant team.</p><div className="mt-10 grid gap-4 md:grid-cols-2">{locations.map(l=><article key={l.id} className="rounded-3xl border bg-card p-6"><h2 className="text-2xl font-black">{l.name}</h2><div className="mt-5 flex flex-col gap-3 text-sm text-muted-foreground"><span className="flex gap-3"><MapPin className="text-primary" size={18}/>{l.address}</span><span className="flex gap-3"><Phone className="text-primary" size={18}/>{l.phone}</span><span className="flex gap-3"><Clock className="text-primary" size={18}/>{l.hours}</span></div><div className="mt-6 flex flex-wrap gap-2"><a href={l.mapsUrl||'#'} className="rounded-full border px-4 py-2 text-sm font-bold"><ExternalLink className="mr-1 inline" size={14}/> Maps</a><a href="#" className="rounded-full bg-primary px-4 py-2 text-sm font-bold text-primary-foreground">Order here</a></div></article>)}</div></div></Shell>}
